services:
  - type: web
    name: uae-legal-agent-backend
    env: python
    plan: free
    rootDir: .
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
